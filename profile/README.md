Each action reads what already happened in your repository, renders it as a self-contained SVG, and commits it back — so your README has something worth looking at.

No service to sign up for, no runtime to install; just a static Crystal binary in a prebuilt image.
